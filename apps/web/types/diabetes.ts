export type diabetesType = {
  glucose:number,
  context:string,
  language:string,
  systolic?: number;   
  diastolic?: number;  
  heartRate?: number;  
  lastMealTime?: string;
mealType?: string;
 exerciseRecent?: string;
  exerciseIntensity?: string;
};